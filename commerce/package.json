{
  "name": "black-sheep-commerce-api",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "npm run catalog:check && npm run cart:test && npm run legal:test && npm run typecheck && npm run db:migrate:local && npm run test && npm run dry-run",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "db:migrate:local": "wrangler d1 migrations apply DB --local --env staging",
    "db:migrations:list:staging": "wrangler d1 migrations list black-sheep-commerce-staging --remote --env staging",
    "db:migrate:staging": "wrangler d1 migrations apply black-sheep-commerce-staging --remote --env staging",
    "db:migrations:list:production": "wrangler d1 migrations list black-sheep-commerce-prod --remote",
    "db:migrate:production": "wrangler d1 migrations apply black-sheep-commerce-prod --remote",
    "dry-run": "wrangler deploy --env staging --dry-run --outdir .wrangler-dist",
    "dev": "wrangler dev --env staging",
    "preview": "wrangler preview --env staging",
    "deploy:staging": "npm run db:migrate:staging && wrangler deploy --env staging",
    "deploy:production": "npm run db:migrate:production && wrangler deploy",
    "catalog:build": "node ../scripts/build-commerce-catalog.mjs",
    "catalog:check": "node ../scripts/build-commerce-catalog.mjs --check",
    "cart:test": "node ../scripts/test-cart-core.mjs",
    "legal:test": "node ../scripts/test-commerce-legal.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^3.2.0",
    "wrangler": "^4.135.0"
  }
}
