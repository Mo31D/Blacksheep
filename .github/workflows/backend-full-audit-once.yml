name: Backend Full Audit Once

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/backend-full-audit-once.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  backend-full-audit:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    defaults:
      run:
        working-directory: commerce
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: commerce/package.json

      - name: Install commerce dependencies
        run: npm install --no-audit --no-fund

      - name: Full Commerce CI-equivalent validation
        run: npm run check

      - name: Verify current static/search contract
        working-directory: .
        run: node scripts/verify-search-readiness.mjs

      - name: Audit stale pre-cutover references
        working-directory: .
        shell: bash
        run: |
          {
            echo "=== stale pre-cutover references in active workflow/runtime files ==="
            git grep -n -E "0008_concurrency_guards\.sql|pre-Product-Core|Production remains pre|Production isolation" -- .github/workflows commerce/scripts commerce/src || true
            echo
            echo "=== legacy category builder references ==="
            git grep -n -E "build-icecream\.mjs|build-romneys\.mjs" -- .github/workflows commerce scripts README.md || true
            echo
            echo "=== TODO/FIXME/HACK markers in commerce runtime ==="
            git grep -n -E "\b(TODO|FIXME|HACK)\b" -- commerce/src commerce/scripts || true
          } | tee /tmp/backend-static-audit.txt

      - name: Verify post-cutover source configuration
        shell: bash
        run: |
          node - <<'NODE'
          const fs=require("fs");
          const config=JSON.parse(fs.readFileSync("wrangler.jsonc","utf8"));
          const vars=config.vars||{};
          const r2=config.r2_buckets||[];
          const crons=config.triggers?.crons||[];
          const site=fs.readFileSync("../assets/site.js","utf8");
          const failures=[];
          for(const key of ["ORDER_RESERVATIONS_ENABLED","D1_PUBLIC_CATALOG_ENABLED","D1_COMMERCE_AUTHORITY_ENABLED"]){
            if(vars[key]!=="true") failures.push(key+" not enabled");
          }
          if(!r2.some(x=>x.binding==="PRODUCT_MEDIA"&&x.bucket_name==="black-sheep-product-media-prod")){
            failures.push("Production PRODUCT_MEDIA binding missing");
          }
          if(!crons.includes("*/30 * * * *")) failures.push("Production reservation cron missing");
          if(!site.includes("const BLACK_SHEEP_PRODUCTION_LIVE_COMMERCE=true;")){
            failures.push("Production live-commerce marker not enabled");
          }
          if(failures.length){
            console.error(failures);
            process.exit(1);
          }
          console.log("Post-cutover source configuration passed.");
          NODE

      - name: Require Cloudflare GitHub Actions credentials
        shell: bash
        run: |
          test -n "$CLOUDFLARE_API_TOKEN"
          test -n "$CLOUDFLARE_ACCOUNT_ID"

      - name: Read-only Production D1 integrity
        shell: bash
        run: |
          npx wrangler d1 execute black-sheep-commerce-prod --remote --json             --command "SELECT name FROM d1_migrations ORDER BY id DESC LIMIT 1; SELECT COUNT(*) AS active_products FROM products WHERE publication_status='ACTIVE' AND current_published_version_id IS NOT NULL; SELECT COUNT(*) AS active_variants FROM product_variants v JOIN products p ON p.id=v.product_id WHERE p.publication_status='ACTIVE' AND v.is_default=1 AND v.active=1; SELECT COUNT(*) AS orders FROM orders; SELECT COUNT(*) AS active_reservations FROM inventory_reservations WHERE state IN ('ACTIVE','COMMITTED');"             > /tmp/backend-prod-d1.json
          cat /tmp/backend-prod-d1.json
          node - /tmp/backend-prod-d1.json <<'NODE'
          const fs=require("fs");
          const b=JSON.parse(fs.readFileSync(process.argv[2],"utf8"));
          const latest=String(b?.[0]?.results?.[0]?.name||"");
          const products=Number(b?.[1]?.results?.[0]?.active_products??-1);
          const variants=Number(b?.[2]?.results?.[0]?.active_variants??-1);
          const orders=Number(b?.[3]?.results?.[0]?.orders??-1);
          const reservations=Number(b?.[4]?.results?.[0]?.active_reservations??-1);
          if(latest!=="0012_order_returns.sql"||products!==146||variants!==146||orders<4||reservations<0){
            console.error({latest,products,variants,orders,reservations});
            process.exit(1);
          }
          console.log("Production D1 integrity passed.",{latest,products,variants,orders,reservations});
          NODE

      - name: Read-only Production health and Admin reachability
        shell: bash
        working-directory: .
        run: |
          HEALTH="$(curl --fail --silent --show-error --max-time 30 https://api.theblacksheepshop.co.uk/health)"
          echo "$HEALTH"
          node - "$HEALTH" <<'NODE'
          const h=JSON.parse(process.argv[2]);
          const failures=[];
          if(h.status!=="ok") failures.push("status");
          if(h.environment!=="production") failures.push("environment");
          if(h.database!=="bound") failures.push("database");
          if(h.features?.orderReservations!==true) failures.push("orderReservations");
          if(h.features?.publicCatalog!==true) failures.push("publicCatalog");
          if(h.features?.commerceAuthority!==true) failures.push("commerceAuthority");
          if(h.notifications?.provider!=="resend") failures.push("resend");
          if(h.notifications?.fromConfigured!==true) failures.push("fromConfigured");
          if(h.notifications?.ownerConfigured!==true) failures.push("ownerConfigured");
          if(h.notifications?.webhookConfigured!==true) failures.push("webhookConfigured");
          if(failures.length){console.error(failures);process.exit(1)}
          NODE
          curl --fail --silent --show-error --max-time 30 https://api.theblacksheepshop.co.uk/admin > /tmp/backend-prod-admin.html
          test -s /tmp/backend-prod-admin.html
          grep -qi "Black Sheep" /tmp/backend-prod-admin.html
          echo "Production Admin route reachable."

      - name: Verify Production public D1 catalogue contract and parity
        shell: bash
        run: |
          curl --fail --silent --show-error --max-time 60             "https://api.theblacksheepshop.co.uk/v1/catalog?limit=200"             > /tmp/backend-prod-catalog.json
          node - /tmp/backend-prod-catalog.json <<'NODE'
          const fs=require("fs");
          const p=JSON.parse(fs.readFileSync(process.argv[2],"utf8"));
          if(!Array.isArray(p.products)||p.products.length!==146){
            console.error("Unexpected Production public catalogue.",{count:p.products?.length});
            process.exit(1);
          }
          for(const product of p.products){
            for(const privateField of ["costMinor","barcode","supplier","supplierUrl","audit","internalNote","currentDraftVersionId"]){
              if(Object.prototype.hasOwnProperty.call(product,privateField)){
                console.error("Private field leaked.",{id:product.id,privateField});
                process.exit(1);
              }
            }
          }
          console.log("Production public catalogue contract passed.",p.products.length);
          NODE
          PHASE6_PUBLIC_CATALOG_ENDPOINT=https://api.theblacksheepshop.co.uk npm run phase6:catalog-parity

      - name: Confirm staging schema before E2E
        shell: bash
        run: |
          npx wrangler d1 execute black-sheep-commerce-staging --remote --env staging --json             --command "SELECT name FROM d1_migrations ORDER BY id DESC LIMIT 1; SELECT COUNT(*) AS active_products FROM products WHERE publication_status='ACTIVE' AND current_published_version_id IS NOT NULL;"             > /tmp/backend-staging-baseline.json
          cat /tmp/backend-staging-baseline.json
          node - /tmp/backend-staging-baseline.json <<'NODE'
          const fs=require("fs");
          const b=JSON.parse(fs.readFileSync(process.argv[2],"utf8"));
          const latest=String(b?.[0]?.results?.[0]?.name||"");
          const products=Number(b?.[1]?.results?.[0]?.active_products??-1);
          if(latest!=="0012_order_returns.sql"||products!==146){
            console.error({latest,products});
            process.exit(1);
          }
          NODE

      - name: Clean failed-audit orphan rows from the previous run
        shell: bash
        run: |
          IDS="'e2e-36232650278','e2e-cancel-36232650278'"
          npx wrangler d1 execute black-sheep-commerce-staging --remote --env staging --json \
            --command "SELECT COUNT(*) AS reservation_items FROM inventory_reservation_items iri JOIN inventory_reservations ir ON ir.id=iri.reservation_id WHERE ir.order_id IN ($IDS); SELECT COUNT(*) AS movements FROM inventory_movements WHERE order_id IN ($IDS);" \
            > /tmp/backend-orphan-safety.json
          cat /tmp/backend-orphan-safety.json
          node - /tmp/backend-orphan-safety.json <<'NODE'
          const fs=require("fs");
          const b=JSON.parse(fs.readFileSync(process.argv[2],"utf8"));
          const reservationItems=Number(b?.[0]?.results?.[0]?.reservation_items??-1);
          const movements=Number(b?.[1]?.results?.[0]?.movements??-1);
          if(reservationItems!==0||movements!==0){
            console.error("Refusing orphan cleanup: immutable inventory evidence exists.",{reservationItems,movements});
            process.exit(1);
          }
          console.log("Orphan cleanup safety gate passed.");
          NODE
          npx wrangler d1 execute black-sheep-commerce-staging --remote --env staging --json \
            --command "DELETE FROM order_messages WHERE order_id IN ($IDS); DELETE FROM customer_review_tokens WHERE order_id IN ($IDS); DELETE FROM refunds WHERE order_id IN ($IDS); DELETE FROM order_adjustments WHERE revision_id IN (SELECT id FROM order_revisions WHERE order_id IN ($IDS)); DELETE FROM inventory_reservations WHERE order_id IN ($IDS); DELETE FROM order_revision_items WHERE revision_id IN (SELECT id FROM order_revisions WHERE order_id IN ($IDS)); DELETE FROM order_revisions WHERE order_id IN ($IDS); DELETE FROM order_events WHERE order_id IN ($IDS); DELETE FROM order_items WHERE order_id IN ($IDS); DELETE FROM orders WHERE id IN ($IDS);"
          npx wrangler d1 execute black-sheep-commerce-staging --remote --env staging --json \
            --command "SELECT COUNT(*) AS remaining_orders FROM orders WHERE id IN ($IDS); SELECT COUNT(*) AS remaining_reservations FROM inventory_reservations WHERE order_id IN ($IDS);" \
            > /tmp/backend-orphan-cleanup-result.json
          cat /tmp/backend-orphan-cleanup-result.json
          node - /tmp/backend-orphan-cleanup-result.json <<'NODE'
          const fs=require("fs");
          const b=JSON.parse(fs.readFileSync(process.argv[2],"utf8"));
          if(Number(b?.[0]?.results?.[0]?.remaining_orders??-1)!==0||Number(b?.[1]?.results?.[0]?.remaining_reservations??-1)!==0){
            console.error("Failed-audit orphan cleanup did not complete.",b);
            process.exit(1);
          }
          NODE

      - name: Run Admin V2 staging E2E
        run: node scripts/staging-v2-e2e.mjs

      - name: Run customer-review edge-case staging E2E
        run: node scripts/staging-review-edge-e2e.mjs

      - name: Install ephemeral browser audit dependency
        run: npm install --no-save --package-lock=false playwright@1.55.0

      - name: Install Chromium and WebKit
        run: npx playwright install --with-deps chromium webkit

      - name: Run shopping basket / checkout browser E2E
        run: node scripts/staging-checkout-browser-e2e.mjs

      - name: Run Admin browser QA
        run: node scripts/staging-admin-browser-qa.mjs

      - name: Run storefront live-overlay browser QA
        run: node scripts/staging-storefront-overlay-qa.mjs

      - name: Upload browser and audit artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: backend-full-audit-artifacts
          path: |
            commerce/browser-e2e-artifacts
            commerce/admin-browser-qa-artifacts
            commerce/storefront-overlay-qa-artifacts
            /tmp/backend-static-audit.txt
            /tmp/backend-prod-d1.json
            /tmp/backend-staging-baseline.json
          if-no-files-found: ignore
          retention-days: 7

      - name: Safety statement
        if: always()
        run: |
          echo "Production checks in this workflow are read-only."
          echo "Browser/order mutations target staging only and use existing QA cleanup logic."
          echo "No Production deploy, migration, INSERT, UPDATE, DELETE, secret mutation or rollback command is present."
